# OneBullEx Android QA Automation Contract

This contract records the stable assumptions shared by QA flows, Android
developers, and agents that maintain this Skill.

## Canonical Source

- Repo source: `workflow/skills/qa/onebullex-android-qa`
- Codex mirror: `/Users/jingxing/.codex/skills/onebullex-android-qa`
- Cursor mirror: `/Users/jingxing/Desktop/Onebullex/.cursor/skills/onebullex-android-qa`

Edit the repo source only. Runtime mirrors are generated by
`scripts/sync-onebullex-android-qa-skill.sh`.

## Target App

- Default device: `SM02G4061923909` Solana Seeker
- Default channel: `dev`
- Dev package: `com.onemore.onebullex.dev`
- Prod package: `com.onemore.onebullex`
- Default activity: `com.icy.neptune.MainActivity`
- Default VPN package when required: `com.follow.clash`

Every non-dry-run must pass the APK version gate or record explicit human
acceptance of the package-gate risk.
Every non-dry-run must also pass the `adb devices -l` stability gate before the
runner is allowed to continue.

## Locator Policy

Selector priority:

1. `resource-id`
2. `content-desc`
3. exact `text`
4. regex `text` or `content-desc`
5. coordinate fallback

New Android surfaces should expose stable IDs. For Compose screens, enable
`testTagsAsResourceId = true` at the app root and set stable `testTag` values.
Use automation IDs for testing contracts and reserve human `contentDescription`
for accessibility where possible.

## Route Map

Reusable page controls live in `routes/*.yaml`. Flows should reference route
keys with `route: feature.element` instead of repeating selector and coordinate
fallback data in every step.

Route entries may contain:

- `selector`: uiautomator selector used by `tap`, `input`, or wait-like actions
- `coordinate`: temporary fallback coordinate for `tap_xy`
- `description`: human-readable context
- `stability`: `stable`, `text`, `fallback`, or `unknown`
- `needs_test_id`: true when Android should add a stable automation ID

When a flow run uses coordinate fallback, the learning loop should generate a
route update candidate rather than treating the coordinate as a permanent
solution.

## Safety

Login credentials must come from environment variables. Trading or order flows
must stop before final side effects unless the user explicitly confirms account,
environment, symbol, direction, quantity, leverage, and final action.

## Recording And Release

Record & Replay artifacts are exploratory inputs only. Before a recorded path is
treated as stable QA coverage, convert it into selector-driven routes and flows,
then validate it through the normal runner.

Confirmed Skill improvements are written to the repo source first, synced to the
runtime mirrors second, and released through a branch + PR flow rather than
direct `main` pushes by default.
